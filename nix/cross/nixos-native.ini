# Meson native build file for NixOS
# Usage: meson setup builddir --native-file cross/nixos-native.ini
#
# This file can specify NixOS-specific paths and settings
# In most cases, Meson will auto-detect settings correctly on NixOS

[binaries]
# Let NixOS environment provide the compilers
# These are typically set via nix-shell or nixpkgs

[properties]
# NixOS-specific properties can be added here
# For example, specific include paths or library paths
